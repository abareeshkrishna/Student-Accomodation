doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Form Styles
    style.
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
      }

      .container {
        width: 80%;
        margin: 0 auto;
      }

      h1 {
        text-align: center;
      }

      form {
        margin-top: 20px;
      }

      label {
        display: block;
        margin-bottom: 5px;
      }

      input[type="text"],
      input[type="number"],
      textarea {
        width: 100%;
        padding: 8px;
        margin-bottom: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-sizing: border-box;
      }

      textarea {
        resize: vertical;
      }

      input[type="date"] {
        width: 100%;
        padding: 8px;
        margin-bottom: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-sizing: border-box;
      }

      button {
        padding: 10px 20px;
        background-color: #4CAF50;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
      }

      button:hover {
        background-color: #45a049;
      }

      button[type="submit"] {
        width: 100%;
      }
  body
    include header
    div.container
      h1 Rental property Form
      form(action="/rental_property" method="post")
        label(for="title") Title:
        input(type="text", id="title", name="title", required)
        
        label(for="description") Description:
        textarea(id="description", name="description", rows="4", required)
        
        //- label(for="agency_id") agency ID:
        //- input(type="number", id="agency_id", name="agency_id", required)
        
        label(for="location") Location:
        input(type="text", id="location", name="location")
        
        label(for="rent") Rent:
        input(type="text", id="rent", name="rent")
        
        label(for="posted_date") Posted Date:
        input(type="date", id="posted_date", name="posted_date", required)
        
        button(type="submit") Rental Property
